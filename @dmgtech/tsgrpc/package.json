{
  "name": "@dmgtech/tsgrpc",
  "version": "0.9.0",
  "main": "dist/tsgrpc.js",
  "types": "dist/tsgrpc.d.ts",
  "files": [
    "dist",
    "!dist/.tsbuildinfo"
  ],
  "scripts": {
    "prepublish": "tsc -b tsconfig.json",
    "build": "tsc -b tsconfig.json",
    "test": "jest --coverage --no-cache"
  },
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.5",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "@dmgtech/tsgrpc-protobuf-codec": "workspace:@dmgtech/tsgrpc-protobuf-codec"
  }
}
